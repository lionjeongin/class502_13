package exam02;

public class Ex02 {
    public static void main(String[] args) {
       /* C c = new C();
        // 생성자는 함수(메서드다) 자료(memory)가 필요하다. 함수가 필요한 자료 = static
        A a = c; //c 객체가 담겼지만 A 클래스에서 담을 수 있는 크기 내에서 자원을 접근한다.
        B b = c; //c 객체가 담겼지만 B 클래스에서 담을 수 있는 크기 내에서 자원을 접근한다.
        // 자바 입장에서도 출처를 명확하게 알기 때문에 자동형변환 된다.!!

        */

        A a = new C(); // C 객체다.
    }
}
